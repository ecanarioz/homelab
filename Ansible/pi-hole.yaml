---
- name: Update Pi-hole
  hosts: all
  become: yes

  tasks:
    - name: Update package lists
      apt:
        update_cache: yes

    - name: Update Pi-hole
      shell: "pihole -up"

    - name: Update blocklists
      shell: "pihole -g"
